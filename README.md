Created Keygen using ssh-keygen -t ed25519 -C "EMAIL_ADD" for vercel.
Add key to public key to github.
Add private key as a env var named SSH_PRIVATE_KEY.
[ssh -T git@github.com] to test connection from terminal.
[chmod +x build.sh] to make file executable.
